# 业务 skill（L2）

项目级业务规则，与 OfficeCLI 官方能力 skill（`load_skill pptx` 等）分离。

| 目录 | 说明 |
|------|------|
| `business-ppt/` | 通用 PPT 业务（占位，待补充） |

`python -m src.main --init-workspace` 会将本目录复制到 `.ppt-agent/skills/`。
