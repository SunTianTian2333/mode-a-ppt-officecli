# mode-a-ppt-officecli · 验收清单

> Spec：[`spec.md`](./spec.md)  
> 架构：[`架构.md`](./架构.md)

## P0 · 环境

- [x] AC-P0-1：`officecli --version` 可执行（≥ 1.0.146）
- [x] AC-P0-2：CLI 可 `create` / `validate` pptx（`output/p0-smoke.pptx`）
- [x] AC-P0-3：MCP Server stdio `tools/call` 可返回版本或 stats

## Step 1 · 项目骨架

- [x] AC-S1-1：`pip install -e ".[dev]"` 成功
- [x] AC-S1-2：`python -m src.main` 打印 config smoke（`ready=step1`）
- [x] AC-S1-3：`prompts/system.md` · `knowledge/README.md` 占位存在
- [x] AC-S1-4：`tests/test_config.py` 通过

## Step 2 · MCP Client

- [x] AC-S2-1：`python -m src.mcp_smoke` → `tools=['officecli']`，`ready=step2`
- [x] AC-S2-2：MCP 调用可 `create` → `output/step2-smoke.pptx`
- [x] AC-S2-3：`tests/test_mcp_client.py` 通过
- [x] AC-S2-4：不依赖 Cursor / `~/.cursor/mcp.json`

## Step 3 · Skill 运行时加载

- [x] AC-S3-1：`prompts/system.md` 含 Skill 路由表；改 pptx 前必须 `load_skill`
- [x] AC-S3-2：`python -m src.skill_smoke` → `load_skill_pptx` / `pitch-deck` / `list` OK，`ready=step3`
- [x] AC-S3-3：无静态 `knowledge/pptx_skill.md`；`tests/test_skill_load.py` 通过
- [x] AC-S3-4：`scripts/verify-load-skill.sh` · README / Spec 记录验证命令

## Step 4 · LangChain Agent

- [x] AC-S4-1：`run_ppt_agent()` 在 `officecli_tools_session()` 内 `build_agent()` + `ChatOpenAI`
- [x] AC-S4-2：CLI 入口 `python -m src.main "<用户话>"`（无参仍 step1 smoke）
- [ ] AC-S4-3：至少 3 页带文字静态 deck（A-PPT-1 最小 Demo；需 `OPENAI_API_KEY` 实测）
- [x] AC-S4-4：交付前 prompt 含 Delivery Gate（validate / issues / save）
- [ ] AC-S4-5：能口头对照 L1 Q1–Q2（loop + tool 分离）与 mini-harness 差异

## 概念验收（Step 4 后 oral）

- [ ] Q1：画出 User → Agent → MCP → officecli → pptx 数据流  
- [ ] Q2：`command` 字符串 tool 与结构化 tool 的利弊  
- [ ] Q3：`prompts/` vs `knowledge/` 分工  
- [ ] Q4：为何不用 Cursor 当 Agent  

## 文档验收（每 Step 完成）

- [ ] Spec AC 勾选 + 实际交付已填  
- [ ] 本清单对应节勾选  
- [ ] 项目 README Phase 表同步  
- [ ] 文档与 README / 验收清单同步  

---

最后更新：2026-09-02（Step 4 代码就绪；A-PPT-1 Demo 待 API Key 实测）
